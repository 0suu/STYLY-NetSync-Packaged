diff --git a/styly_netsync/logging_utils.py b/styly_netsync/logging_utils.py
index a3c2798..7a4373c 100755
--- a/styly_netsync/logging_utils.py
+++ b/styly_netsync/logging_utils.py
@@ -1,11 +1,9 @@
 # logging_utils.py
-from __future__ import annotations
-
-import logging
 import sys
+import logging
 import threading
 from collections.abc import Callable
-from datetime import time, timedelta
+from datetime import timedelta
 from pathlib import Path
 from typing import Any
 
@@ -19,9 +17,9 @@ from loguru._ctime_functions import get_ctime
 LOG_ROTATION_SIZE_BYTES = 10 * 1024 * 1024
 LOG_ROTATION_MAX_AGE = timedelta(days=7)
 LOG_RETENTION_MAX_FILES = 20
-DEFAULT_LOG_FILENAME = "netsync-server.log"
-RotationRule = str | int | time | timedelta | Callable[..., bool]
-RetentionRule = str | int | timedelta | Callable[[list[str]], None]
+DEFAULT_LOG_FILENAME = "netsync.log"
+RotationRule = str | int | float | timedelta | Callable[[Any, Any], bool]
+RetentionRule = str | int | float | timedelta | Callable[[list[Any]], Any]
 
 
 class _RotationState:
@@ -58,7 +56,6 @@ class InterceptHandler(logging.Handler):
     """Redirect stdlib logging to loguru."""
 
     def emit(self, record: logging.LogRecord) -> None:
-        level: str | int
         try:
             level = logger.level(record.levelname).name
         except (ValueError, TypeError):
@@ -75,7 +72,7 @@ class InterceptHandler(logging.Handler):
         )
 
 
-def _resolve_log_path(file: Any) -> Path:
+def _resolve_log_path(file) -> Path:
     """Return a Path for the log file supporting loguru handles and Path objects."""
 
     if isinstance(file, Path):
@@ -177,10 +174,14 @@ def configure_logging(
     reset_rotation_state()
     logger.remove()
 
+    # Android (Chaquopy): force disable enqueue mode to avoid multiprocessing
+    # dependency (_multiprocessing / semaphores are often unavailable).
+    enqueue_logs = False
+
     console_kwargs: dict[str, Any] = {
         "level": console_level.upper(),
         "serialize": console_json,
-        "enqueue": True,
+        "enqueue": enqueue_logs,
         "backtrace": False,
         "diagnose": False,
     }
@@ -209,10 +210,11 @@ def configure_logging(
             logger.add(
                 log_file,
                 level="DEBUG",
-                serialize=True,
+                serialize=False,
                 rotation=rotation_rule,
                 retention=retention_rule,
-                enqueue=True,
+                enqueue=enqueue_logs,
+                format="{time:YYYY-MM-DD HH:mm:ss.SSS} | {level: <8} | {message}",
                 backtrace=False,
                 diagnose=False,
             )
